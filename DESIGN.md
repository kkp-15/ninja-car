# 本命 vs 夢枠 車コスト比較 — 設計書

作成 2026-09-14。次のセッションはこの1枚から着手する。経緯と判断理由はメモリ `project_car_compare.md`。

## 何を作るか

検討中の車（**本命**）と憧れの車（**夢枠**）を2枚のカードに並べ、
**月々のローン・月々の維持費・3年後の売却額を引いた「月々の実質負担」**を1画面で比べるツール。

車種を選ぶ維持費シミュレーターは大手（ガリバー・カルモ・car-life.adg7）が既に持つ。
差別化は「**2台並列 × リセール込みの実質負担**」の一点だけ。

## 決まっていること

| 項目 | 決定 |
|---|---|
| 置き場所 | `car.kkpwebninja.com`（リポ `ninja-car`）。apex掲載は別タスク |
| 範囲 | **2台比較の1ページだけ**。車種別ページ・予算逆算ツールは作らない |
| 残価率 | **ガリバー「新車から3年後のリセールバリューランキング TOP30」**（2026-09-02集計）の最小〜最大の幅をスライダーで動かす。初期値は中央 |
| 幅が極端な車 | 外さない（例: ジムニーシエラ 15.1〜122.0%）。幅の広さそのものを表示する |
| 載せる車 | ガリバーTOP30から、新車で買えない3台・ハイラックス・ディフェンダーを除いた**25台**。フェラーリは載らない |
| 保有期間 | **3年固定**（残価データが3年のため） |
| 収益化 | 最初は AdSense のみ（結果の下）。車関連ASPは提携0件。A8で実在・確定率を確認してから |
| 撤退判断 | 公開6〜8週で GSC クリックと GA4 `compare_run` を見て、広げるか止めるか |

### 除外した3台（新車で買えない）
- GT-R: 2025-08-26 生産終了
- スイフトスポーツ: Final Edition の受注が最長 2026-03 末で終了
- マカン（ガソリン）: 2026-07 末 生産終了、秋以降は在庫販売のみ

## 画面

```
┌────────── 本命 ──────────┐ ┌────────── 夢枠 ──────────┐
│ 車種 [ハリアー ▼]          │ │ 車種 [Gクラス ▼]           │
│ 価格 ━━━●━━  371万円     │ │ 価格 ━━●━━━  xxxx万円    │
│ 3年後に売れる割合          │ │ 3年後に売れる割合          │
│ 60.5% ━━●━━ 97.0%  78.8% │ │ 91.4% ● 91.4%   91.4%     │
│ 任意保険（月）[     ]円    │ │ 任意保険（月）[     ]円    │
└──────────────────────┘ └──────────────────────┘
 共通: 頭金 / 金利 / ローン回数 / 月間走行距離 / 駐車場代

 月々ローン      xx,xxx円        xx,xxx円
 月々維持費      xx,xxx円        xx,xxx円
 月々の実質負担  xx,xxx円   ←→   xx,xxx円   （差 月xx,xxx円）
```

- 残価率スライダーの両端に、出典の最小値と最大値をそのまま表示。出典名・集計日はカード下に常時表示
- 共有URLは `?a=harrier&b=g-class`（任意で条件も）。シェアは X・LINE・コピーの3ボタン標準
- デザインは全サイト共通の方針に従う（オレンジ・クリーム系、丸ゴシック、ダークモードなし、装飾絵文字なし）。Gemini案の「💡あるある注記」は使わない
- JSON-LD の `author.name` は「web忍者の砦」

## 計算式

記号: 価格 `P`、頭金 `D`、借入 `L = P − D`、月利 `r = 年利/12`、回数 `n`、3年 = 36か月

**月々ローン**（元利均等）
```
M = L·r / (1 − (1+r)^−n)      （r = 0 のときは L/n）
```
Geminiのサンプルは「価格×1.05÷回数」で、年数を変えても利息が変わらない誤り。使わない。

**3年時点の残債**（n > 36 のとき。n ≤ 36 なら 0）
```
B36 = L·(1+r)^36 − M·((1+r)^36 − 1)/r
```

**3年間の支出**
```
購入時の法定費用 = 重量税(新規3年分) + 自賠責(37か月)
3年の維持費     = 自動車税(年額)×3 + 燃料費×36 + 駐車場×36 + 任意保険×36
売却額          = P × 残価率
実質負担(3年)   = D + M×min(n,36) + B36 + 購入時の法定費用 + 3年の維持費 − 売却額
月々の実質負担  = 実質負担(3年) ÷ 36
```

- **3年保有では車検は来ない**（新車の初回車検は3年後）。Geminiのコードにあった「毎月の車検積立」は入れない
- 自動車税の購入年の月割は第1段階では扱わず、年額×3で計算して注記する
- 任意保険は年齢・等級で数倍変わるので、車種ごとの固定値は持たない。**未入力なら合計から外し、その旨を表示**
- メンテナンス費は出典の強い目安が無いので、第1段階では持たない（入れるなら入力式）

## 公的な数字（正本 `data/public_rates.json`）

| 項目 | 値 | 出典 |
|---|---|---|
| 自動車税（2019/10以降登録） | 1.0L以下 25,000／〜1.5L 30,500／〜2.0L 36,000／〜2.5L 43,500／〜3.0L 50,000／〜3.5L 57,000／〜4.0L 65,500／〜4.5L 75,500／〜6.0L 87,000／6.0L超 110,000 | [総務省](https://www.soumu.go.jp/main_sosiki/jichi_zeisei/czaisei/131410.html) |
| 重量税 自家用乗用 | 0.5tごと年 4,100円（当分の間）／2,500円（本則・エコカー等）。新規登録時に3年分 | [財務省 131.pdf](https://www.mof.go.jp/tax_policy/summary/consumption/131.pdf)（令和8年5月〜令和9年4月の制度） |
| 自賠責 自家用乗用 37か月 | 24,190円 → **2026-11-01から 25,180円** | [JAF Mate](https://jafmate.jp/car/traffic_topics_20260702_1199387.html)（損害保険料率算出機構） |
| レギュラーガソリン | 170.0円/L（2026-09-07時点） | [資源エネルギー庁](https://www.enecho.meti.go.jp/statistics/petroleum_and_lpgas/pl007/results.html) |

## 車種データ（`data/cars.json`）

1台につき次を持つ。**すべてメーカー公式の出典URLと確認日つき**。

| キー | 内容 |
|---|---|
| `id` | URL用（例 `harrier`） |
| `name` / `maker` | ガリバー表記と対応づける |
| `grade` | 代表グレード名 |
| `powertrain` | ガソリン / ハイブリッド / PHEV / ディーゼル |
| `fuel` | レギュラー / ハイオク / 軽油 |
| `price_yen` | 代表グレードの税込メーカー希望小売価格 |
| `displacement_cc` | 総排気量 |
| `wltc_km_l` | WLTCモード燃費 |
| `weight_kg` | 車両重量 |
| `weight_tax_rate` | `本則` / `当分の間`（エコカー減税の区分。確認できなければ `当分の間` とし注記） |
| `resale3y_min` / `resale3y_max` | ガリバーの値（`data/resale_gulliver_2026-09-02.json` から） |
| `sources` | 諸元表PDF・価格ページのURLと確認日 |

**代表グレードの選び方**: 各車種のエントリーグレード（最も安いグレード）に統一する。上位グレードは価格スライダーで動かしてもらう。
理由は、選び方を人の判断に頼らず再現できるようにするため。ガリバーの残価率は型式混在の幅なので、グレードとは1対1に対応しない。

**集め方**: トヨタは `toyota.jp/pages/contents/<車種>/…/pdf/<車種>_spec_YYYYMM.pdf` に諸元表PDFがある（ハリアーは `harrier_spec_202608.pdf`）。
WebFetch では文字化けするので、PDFを保存して **macOS PDFKit（swift）で文字を取り出す**（pdftoppm は未インストール）。
表が段組みで並ぶので、グレードとの対応は1台ずつ目で確かめる。

## 実装で決めたこと（2026-09-14）

1. サブドメイン: `car`
2. ローン金利の初期値: **年2.55%**（りそなマイカーローン通常金利 1.85/2.55/3.45/4.45% のうち公式の試算例の金利・2026-04-01適用）
3. 燃料単価: 資源エネルギー庁 9/7時点の全国平均 レギュラー170.0／ハイオク180.9／軽油159.5円
4. エコカー減税: **メーカー公式の表記があればそれ、無ければ当分の間税率**。根拠は cars.json の `weight_tax.basis` に1台ずつ書く
   - 財務省131.pdf 2ページ目の区分: 125%/電気自動車等=2回目まで免税、105%=初回免税、100%=75%減、95%=50%減、80%=25%減、80%未達=当分の間。減税は本則税率から計算し100円未満切捨
   - 最初に public_rates.json に書いた区分は1段ずれていた（レクサスISの「80%達成→約7,500円減」で気づき、財務省の図で修正）

### 追加で外した車
- **ハイラックス**: 1ナンバー（貨物）。自動車税・重量税・自賠責の区分が違い、初回車検が2年で3年保有でも来る
- **ディフェンダー**: 公式サイトの重量・燃費は「欧州仕様車値」。日本の国交省審査値を一次資料で確かめられない。出典が出たら追加

### グレードの決め方で注意したこと
- ハリアー・ノア・RAV4・カローラクロスは現行にガソリン車が無い → 最安のハイブリッド車で計算し画面に注記
- アクア・プリウスの「U」はKINTO専用仕様車なので除外（アクアはX、プリウスはX）
- アルファード／ヴェルファイアはガリバーでガソリンとハイブリッドが別名なので、それぞれの最安
- G クラスはガリバーの表記が「メルセデスAMG」なので Mercedes-AMG G 63（公式プライスリストの現行MP202602）

### 検証の記録
- `data/verification_2026-09-14.md`: 収集とは別の経路で出典を開き直して136件照合、不一致0（段組みPDFの列は収集時に全車画像で目視）
- `tests/calc.test.mjs`（19件）: 元利均等・3年時点残債は Decimal の別実装と償還表で参照値を作って固定。重量税はトヨタ・レクサス公式の減税後税額と一致
- `tests/data.test.mjs`（33件）: 出典URL・確認日の欠落、値の範囲、ガリバー元データとの一致、公式の重量税額との一致、個人名・Geminiの数字の混入を検査。GitHub Actions でデプロイ前に実行

## 正確さの運用ルール（最優先・2026-09-14 ユーザー指示「信頼を失わないように」）

お金の判断に使われるツールなので、**数字を1つ間違えるとサイト全体の信頼を失う**。見た目や機能より優先する。

### 1. 数字は一次資料からだけ取る
- 採用してよい出典: メーカー公式（諸元表・価格ページ）、官公庁（総務省・財務省・国交省・資源エネルギー庁）、料率の算出機関、ガリバーの公表データ
- **検索結果の要約・まとめ記事・AIの回答（Gemini含む）の数字は使わない**。見つけた数字は必ず出典の本文で確かめる
  （実例: 自賠責37か月を検索要約は「27,770円」と出したが、本文の正しい値は25,180円）
- 出典で確かめられない数字は**載せない**。推測で埋めない

### 2. すべての数字に出典URLと確認日を持たせる
- `data/*.json` の各値に `sources`（URL・確認日）。出典のない値はビルド時にエラーにする
- 画面には「出典」「集計日・時点」を常時表示（残価率はガリバー・2026-09-02集計、ガソリンは時点つき）

### 3. 集めた数字は別の手順でもう一度確かめる
- 1回目の収集とは別に、**出典を開き直して全件を突き合わせる**（写し間違い・グレード取り違えを見つける）
- 諸元表は段組みなので、特にグレードと価格・重量・燃費の対応を1台ずつ目で確認
- 突き合わせの結果（何台確認・何件修正）を記録に残す

### 4. 計算は手計算の答えとテストで固定する
- 元利均等・3年時点残債・税区分の境界（1,500cc ちょうど等）・0.5t 単位の切り上げを、手で計算した答えと一致させるテストを書く
- 金利0%、頭金=価格、ローン回数≦36 などの端のケースも入れる

### 5. 期限のある数字は日付で切り替え、更新を忘れない
- 自賠責は 2026-11-01 で 24,190円→25,180円（37か月）。**日付で自動的に切り替える**
- 重量税は「令和8年5月1日〜令和9年4月30日」の制度。**期限前に見直す**
- ガソリン単価・残価率は時点ものなので、画面に時点を出し、定期的に更新する（更新日を画面に表示）

### 6. 「目安」であることと限界を正直に書く
- ガリバーの注記「表記のリセールバリュー値での買取を保証するものではございません」をそのまま示す
- 任意保険・メンテ費を含まない場合は、含まないと明記
- 自動車税の購入年の月割、エコカー減税の免税・軽減を簡略化している点も明記
- 実在の車種名を扱うので、断定的な評価（「お得」「損」）はしない。数字の比較だけを出す

### 7. 公開前チェック（全部通るまで公開しない）
- [ ] 27台すべて、出典URLが開けて値が一致する
- [ ] 計算テストが全部通る
- [ ] 画面の出典・時点・注記が表示されている
- [ ] Geminiの未検証の数字（3年落ちポルシェ論・オイル交換3〜5万円・フェラーリの車両保険）が残っていない
- [ ] 誤りを見つけた人が連絡できる導線（統一フッターのお問い合わせ）がある

## 入口と見た目の見直し（2026-09-19）

根拠は競合調査（`~/test/_scripts/rivals/LEDGER.md` の #4・#14・#16・#19）。計算式・データ・初期値は変えていない。

| 項目 | 決定 |
|---|---|
| title / h1 | 検索語で始める。title「車の維持費シミュレーション｜ローン込みの月々と売却額で2台比較【2026年】」、h1「車の維持費シミュレーション」。「本命vs夢枠」は h1 の副題と `og:site_name`・記事のパンくずに残す（サイト名は変えていない） |
| 結果の要約 | h1 のすぐ下に `#sum`（本命 月々／夢枠 月々／差）。`render()` が下の表と同じ `ra.perMonth`・`rb.perMonth` から書く。`.tool` の中だけ `position:sticky` で上に残り、結果の表を過ぎると外れる（広告には重ならない） |
| 共通5欄 | 頭金・金利・回数・走行距離・駐車場代は `<details class="common">`「細かく直す」の中（初期は閉じる）。いまの条件を summary に文字で出す。**input の id・value・`<option selected>` は `scripts/build_articles.mjs` の `toolDefaults()` が正規表現で読むので書き方を変えない** |
| 見た目 | 地は白。本文16px・#333、h2 21px（左4pxオレンジ線＋クリームの帯）、ラベル14px、表14〜15px、注記13px。クリーム・オレンジは見出しの帯・表の見出し・カード上辺など小さい面だけ。入力欄は16px・高さ44pxのまま |
| 計算方法の節 | 「リセールバリュー（3年後に売れる割合）の計算方法」。上の「計算式」をそのまま文章にしたもの。例は選択中の本命の計算結果から出す（新しい数字は持たない） |
| 更新日 | JSON-LD `dateModified`・画面の「更新」（`#pageUpdated`）・`sitemap.xml` の lastmod を同じ日にする。`tests/data.test.mjs` が3つの一致を検査。**数字の確認日（`cars.json` の `_meta.checked`）とは別**で、数字を確かめ直していないのに確認日を進めない |
| シェア | 追加しない（2026-09-15 撤去のまま。テストで戻しを検知） |

## 計測（GA4）

`compare_run`（a, b）／ `resale_slide` ／ `car_change` ／ `common_open`（「細かく直す」を開いた。2026-09-19 追加）（シェアボタンは使われていないため 2026-09-15 に撤去）

## 作り方

- ノービルドの単一HTML ＋ `data/*.json`。正本JSONから画面用データを作る
- 公開は `new-ninja-site` スキル（Cloudflare Pages ＋ GitHub Actions）
- 数字を1つでも変えたら、画面の更新日と `sources` の確認日を揃える
